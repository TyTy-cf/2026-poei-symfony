{# Inside a partial you don't need to extends base.html.twig ! #}

<section class="my-3">
    {% if title is defined %}
        <h2>{{ title }}</h2>
    {% endif %}

    <div class="row">
        {% for user in users %}
            <div class="col-md-4 col-12">

            </div>
        {% endfor %}
    </div>
</section>
