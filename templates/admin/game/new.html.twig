{% extends './admin/base.html.twig' %}

{% block body %}
	<h1>Create new Game</h1>

	{{ include('admin/game/_form.html.twig') }}

	<a href="{{ path('admin_game_index') }}" class='btn btn-secondary'>back to list</a>
{% endblock %}
